---
tags: [setup-pattern, security, secretlint, gitleaks, oneshot-instruction]
---

# setup-pattern導入の経緯 - 開発記録

> **⚠️ 機密情報保護ルール**
>
> このノートに記載する情報について:
> - API キー・パスワード・トークンは必ずプレースホルダー(`YOUR_API_KEY`等)で記載
> - 実際の機密情報は絶対に含めない
> - .env や設定ファイルの内容をそのまま転記しない

**作成日**: 2026-01-22
**関連タスク**: setup-pattern の新規追加

## 問題

session_end.md を AI に渡しても、初手から無視する AI が存在する問題が発生。

**具体的な状況**:
- actions-pattern はトークン削減に非常に効果的
- しかし session_end.md を渡しても、AI の運次第で無視される
- 仕組みでも防御が必要だが、docs-structure にインストール手順を含めるのは得策ではない

**結論**:
- セットアップ手順を独立したパターンとして分離すべき
- メジャーな指示書を一つ一つ置く setup-pattern を用意する

## 試行錯誤

### アプローチA: docs-structure に含める
**試したこと**: docs/setup/ に配置

**結果**: 失敗

**理由**:
- このリポジトリは patterns/ 配下に各パターンを配置する構造
- docs/ は「このリポジトリ自身の開発用ドキュメント」
- docs/setup/ は構造的におかしい

---

### アプローチB: patterns/setup-pattern/ に配置（成功）
**試したこと**: patterns/setup-pattern/ として独立させる

**結果**: 成功

**実装場所**: `patterns/setup-pattern/`

**主なポイント**:
1. 他のパターン（docs-structure, actions-pattern など）と同じ構成
2. templates/ 配下に実際のセットアップガイドを配置
3. npx degit で個別取得可能

---

### アプローチC: setup_secretlint → setup_securecheck へリネーム
**試したこと**: より汎用的な名前に変更

**結果**: 成功

**理由**:
- `setup_secretlint` は具体的なツール名すぎて意味が狭い
- `setup_securecheck` の方が「セキュリティチェック全般」を表現
- 将来的に他のセキュリティツール（Trivy, Snyk など）を追加しても違和感がない

---

### アプローチD: ディレクトリ構造の再編成（成功）
**試したこと**: 今後の拡張性を考慮した構造に変更

**結果**: 成功

**変更内容**:
- `templates/setup_securecheck/` → `setup_securecheck/templates/`
- `templates/setup_securecheck.md` → `setup_securecheck/setup_securecheck.md`
- README を分離：
  - `patterns/setup-pattern/README.md`: setup-pattern 全体の説明
  - `patterns/setup-pattern/setup_securecheck/README.md`: setup_securecheck のワンショット指示

**理由**:
- 今後 `setup_prettier/`, `setup_eslint/` などが増える
- 各セットアップガイドが独立したディレクトリになる
- templates/ は各セットアップガイドの配下に配置する方が自然

## 解決策

### 最終的な構成

```
patterns/setup-pattern/
├── README.md                                   # setup-pattern 全体の説明
└── setup_securecheck/                          # セキュリティチェック導入ガイド
    ├── README.md                               # ワンショット指示（コピペ用）
    ├── setup_securecheck.md                    # 手順書（AI が読むメイン文書）
    └── templates/                              # サンプルファイル集（コピペ元）
        ├── .secretlintrc.json
        ├── .gitleaksignore
        ├── gitleaks.toml
        ├── .husky/pre-commit
        ├── package.json.example
        ├── gitignore.example
        └── scripts/
            ├── secret-scan.sh
            ├── install-gitleaks.sh
            └── pre-commit.js
```

### 役割分担

| ファイル | 役割 |
|---------|------|
| **README.md** | ワンショット指示（npx degit のパスを含む） |
| **setup_securecheck.md** | AI が読む手順書（メイン） |
| **templates/** | サンプルファイル集（補助・コピペ元） |

### 段階的導入の設計

| Phase | 内容 | いつ止めるか |
|-------|------|------------|
| Phase 1 | 初動スキャン（現状把握） | 問題なし → Phase 2 へ |
| Phase 2 | 手動運用（npm scripts） | ピュアなコミット保持したい場合 |
| Phase 3 | pre-commit 強制（全員） | チーム全員で使う場合 |
| Phase 4 | pre-commit 強制（個人用） | 自分だけ使いたい場合 |

### ワンショット指示の実装

docs-structure パターンと同じレベルの README を実装:

1. **冒頭にワンショット指示**（コピペ用）
2. **npx degit のパスを明示的に示す**
3. **解決する問題→特徴→使い方の流れ**
4. **作成される構造を明示**
5. **使用例・運用のコツを具体的に**

```markdown
> **🤖 AIへのワンショット指示（コピペ用）**
>
> ```
> npx degit <username>/my-ai-collaboration-patterns/patterns/setup-pattern/templates/setup_securecheck ./setup_securecheck
>
> setup_securecheck.md を読んで、段階的に導入してください。
> Phase 1 から順に進め、各 Phase で判断が必要な場合は確認してください。
> ```
```

## 学び

### 1. パターンの配置場所は構造的に重要

- **docs/**: このリポジトリ自身の開発用ドキュメント
- **patterns/**: ユーザーが取得して使うパターン集
- この区別を明確にすることで、構造の一貫性が保たれる

### 2. 名前は汎用的にすべき

- 具体的なツール名（setup_secretlint）より、目的を表す名前（setup_securecheck）の方が将来の拡張性が高い
- 複数のツールを組み合わせる場合、汎用的な名前の方が違和感がない

### 3. 手順書とサンプルファイルの役割分担

- **README.md**: ワンショット指示（npx degit のパスを含む）
- **手順書**: AI が読んで段階的に作業する（メイン）
- **サンプルファイル**: コピペ元として参照（補助）
- 手順書内にコード例も含まれているため、サンプルファイルなしでも導入可能

### 4. 段階的導入の重要性

- いきなり自動化せず、Phase 1 で現状把握してから方針決定
- ユーザーの状況（チーム vs 個人、Docker vs ローカルなど）に応じて選択できる
- AI が判断すべきポイントを明記することで、質問攻めを防ぐ

### 5. ディレクトリ構造の拡張性

- 各セットアップガイドが `setup_xxxx/` として独立
- templates/ は各セットアップガイドの配下に配置
- 今後 `setup_prettier/`, `setup_eslint/` などを追加しやすい構造

## 今後の改善案

### 追加候補のセットアップガイド

- `setup_prettier.md` - コードフォーマッター導入
- `setup_eslint.md` - ESLint + TypeScript 環境構築
- `setup_testing.md` - Jest / Vitest テスト環境
- `setup_docker_devcontainer.md` - Dev Container 環境構築
- `setup_ci_github_actions.md` - GitHub Actions CI/CD

### npx degit での部分取得

setup_xxxx/ 配下にコード例を置くことで、以下が可能:

```bash
# 例: メジャーなコード例を持ってくる
npx degit <username>/my-ai-collaboration-patterns/patterns/setup-pattern/templates/setup_securecheck/scripts ./scripts
```

## 関連ドキュメント

- [setup-pattern README](../../../patterns/setup-pattern/README.md)
- [setup_securecheck.md](../../../patterns/setup-pattern/templates/setup_securecheck.md)
- [actions-pattern](../../../patterns/actions-pattern/README.md) - AI への指示テンプレート
- [docs-structure](../../../patterns/docs-structure/README.md) - ドキュメント構造の説明

---

**最終更新**: 2026-01-22
**作成者**: AI (Claude Sonnet 4.5)
