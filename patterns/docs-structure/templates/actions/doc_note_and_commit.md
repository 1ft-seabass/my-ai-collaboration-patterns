ここまでの経緯をノートにまとめ、コミットします。

## 📋 この指示書の理解チェック

作業を開始する前に、以下の内容を理解したことをチェックボックスで提示してください：

- [ ] ノート作成方針をユーザーに提案し、承認を得る
- [ ] docs/notes/TEMPLATE.md をベースにノートを作成する
- [ ] FrontMatter に tags を追加する
- [ ] 命名規則は yyyy-mm-dd-hh-mm-ss-{title}.md 形式
- [ ] 機密情報は必ずプレースホルダーで記載する
- [ ] git status と git diff で変更内容を確認する
- [ ] git log で従来のコミットスタイルを把握する
- [ ] ユーザーに AI 署名・プレフィックス・言語を確認する
- [ ] 段階的なコミット計画を提示し、承認を得る
- [ ] プッシュはせず、コミットのみで完了する

理解できましたか？ゴーサインをください。

---

⚠️ 機密情報保護ルール

ノートとコミットに記載する情報について

- API キー・パスワード・トークンは必ずプレースホルダー(`YOUR_API_KEY`等)で記載
- 実際の機密情報は絶対に含めない
- .env や設定ファイルの内容をそのまま転記しない
- コミット前に git diff で内容を確認
- プッシュはせずコミットのみ(人間がレビュー後にプッシュ)

## 実行手順

1. ノート作成方針の提案
   - ここまでの経緯をノートにまとめる方針を考え、ユーザーに提案
   - 全体の経緯だけでなく小分けにしたノートを作成したほうが良い場面もあるので、ユーザーに提案
   - セッションの流れでノートをすでに作成してるなど作る必要がないときも、ユーザーに提案
   - ユーザーの承認を得る

2. テンプレートの確認
   - docs/notes/TEMPLATE.md を読んで、構造とガイドを把握
   - 命名規則: yyyy-mm-dd-hh-mm-ss-{title}.md
   - FrontMatter に tags を追加（3-5個推奨）

3. ノートの作成
   - 承認された方針に従って作成
   - 機密情報は必ずプレースホルダーで記載
   - FrontMatter を先頭に追加

4. 変更内容の確認
   - `git status` で変更ファイルを確認
   - `git diff` で変更内容を確認
   - 機密情報（APIキー、パスワード、トークンなど）が含まれていないかチェック

5. 従来のコミットログを参考にスタイルを把握
   - `git log --oneline -10` で最近のコミットを確認
   - コミットメッセージの形式（feat:/fix:/docs: など）
   - 日本語 or 英語
   - AI 署名の有無（`Co-Authored-By: Claude` があるか）

6. ユーザーにコミットスタイルを確認
   - このリポジトリは AI 署名を入れますか？ [y/N]
   - コミットメッセージのプレフィックスは？（feat/fix/docs/refactor など）
   - 日本語 or 英語？

7. コミット計画の提案と承認
   - ノートと開発内容を段階的にコミットする計画を提示
   - ユーザーの承認を得る

8. ノートのコミット
   - 作成したノートをコミット
   - 確認したスタイルに従ってコミットメッセージを作成
   - コミット前に必ず git diff で内容を確認

9. 開発内容のコミット（必要に応じて）
   - 承認された計画に従って段階的にコミット
   - 確認したスタイルに従ってコミットメッセージを作成
   - コミット前に必ず git diff で内容を確認

10. 完了報告
    - コミットした内容を簡潔に報告
    - プッシュはせず、コミットのみで完了
